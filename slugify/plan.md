- get the string
- lowerCase().trim()
- split()
- create a new string
- loop through array
    
- if the char >= a && <= z then add it to new string 
- if it's >=0 && <= 9 then add it too.
- if it's "-" || " " then add "-" if the last char is not "-"